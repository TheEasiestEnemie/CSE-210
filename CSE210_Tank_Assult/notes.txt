classes:
        
    (Garrett)Obj: {
        // Encapsulation
        float velocity
        Vector2 position
        float sizeX
        float sizeY
        Circle model

        Obj() {}

        Obj(x, y, sizeX, sizeY) {}

    }

    (Jack)Line: { // needed for alignment
        virtual GetSlope() {} // Abstraction/Polymorphism/Encapsulation
    } 

    (Jack)PlayerTank: Obj {}

    (Garrett)Enemy: Obj {
        (MoveNext, Shoot (virtual)) // Polymorphism
    }

    // Inheritance
    (Garrett) Range: Enemy {}
    (Garrett) Speed: Enemy {}
    (Garrett) Generic: Enemy {}

    (Jack)bullet:Obj {
        MoveNext(currentPosition, slope) {}
    }
    

folders:
    Casting:
    Services:
        // Abstraction
        (Garrett)keyboardService
        (Garrett)MouseService 
        VideoService
    Scripting:
        // Interfacing
        Action
        (Jack)CheckPlayerCollisionsAction: Action
            (Including bounds)
        (Jack)CheckBulletCollisionsAction: Action
        (Jack)ControlPlayerAction: Action
        (Jack)MovePlayerAction: Action
        (Jack)MoveEnemiesAction: Action
        (Jack)MoveBulletsAction: Action
    Director:
    Images:
        Stores all images used in the game
